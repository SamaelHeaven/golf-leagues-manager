import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "ca.qc.cegepst.golf",
    appName: "golf-leagues",
    webDir: "public"
};

export default config;
